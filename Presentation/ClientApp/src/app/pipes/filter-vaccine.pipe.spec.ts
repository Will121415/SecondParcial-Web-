import { FilterVaccinePipe } from './filter-vaccine.pipe';

describe('FilterVaccinePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVaccinePipe();
    expect(pipe).toBeTruthy();
  });
});
